# My API Postman Collection

This repository contains a Postman collection for testing the XYZ API.

## Usage
1. Import `Restfulbooker API.postman_collection.json` into Postman.
2. (Optional) Import the `URL_SHORTCUT.postman_environment.json`.
3. Set your API key or token in the environment variables.
4. Run the collection manually.

